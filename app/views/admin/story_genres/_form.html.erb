<%= form_with model:story_genre, url: url do |f| %>
	<%= render 'layouts/error_messages', resource: story_genre %>
	<%= f.label :story, "ジャンル" %>
	<%= f.text_field :story %>
	<%= f.submit submit, class: "btn btn-primary" %>
<% end %>