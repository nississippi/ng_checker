<%= form_with model:ng_genre, url: url do |f| %>
	<%= render 'layouts/error_messages', resource: ng_genre %>
	<%= f.label :ng_genre, "ジャンル名:" %>
	<%= f.text_field :ng_genre %>
	<%= f.submit submit, class: "btn btn-primary" %>
<% end %>