<%= form_with model:media_genre, url: url do |f| %>
	<%= render 'layouts/error_messages', resource: media_genre %>
	<%= f.label :media, "媒体" %>
	<%= f.text_field :media %>
	<%= f.submit submit, class: "btn btn-sm btn-primary" %>
<% end %>