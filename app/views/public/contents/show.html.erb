<div class="container mt-5">

  <h1><%= @content.title %></h1>

	<div class="row">
		<div class="col-lg-6 col-md-8 col-sm-12">
			<table class="table mt-3">
				<thead>
					<tr>
						<th>NG一覧</th>
					</tr>
				</thead>
				<tbody>
					<% @ng_expressions.each do |ng| %>
						<tr>
							<td>
								<%= ng.ng_expression %>
							</td>
							<td>
								<%= link_to ng_expression_votes_path(ng), method: :post do %>
        					出てくる
      					<% end %>
								どちらとも言えない
								出てこない
							</td>
						</tr>
					<% end %>
				</tbody>
			</table>
		</div>
	</div>
</div>